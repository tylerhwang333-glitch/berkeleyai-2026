services:
  redis:
    image: redis/redis-stack:latest
    container_name: cs2coach-redis
    ports:
      - "6379:6379"   # Redis
      - "8001:8001"   # RedisInsight UI
    volumes:
      - redis_data:/data

volumes:
  redis_data:
